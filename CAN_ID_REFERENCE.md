# THE ENGINE — CAN ID Reference
## FRC Team 2950 | Hardware-verified from swerve-test branch

> **Source of truth:** `hardware_config.ini` → regenerated by `python tools/generate_configs.py`
> Last updated: 2026-03-30

---

## Swerve Drive

| Device | Type | CAN ID |
|--------|------|--------|
| Front Left Drive  | NEO Brushless via SPARK MAX | **13** |
| Front Left Steer  | NEO Brushless via SPARK MAX | **5**  |
| Front Right Drive | NEO Brushless via SPARK MAX | **7**  |
| Front Right Steer | NEO Brushless via SPARK MAX | **19** |
| Back Left Drive   | NEO Brushless via SPARK MAX | **10** |
| Back Left Steer   | NEO Brushless via SPARK MAX | **6**  |
| Back Right Drive  | NEO Brushless via SPARK MAX | **8**  |
| Back Right Steer  | NEO Brushless via SPARK MAX | **12** |

**Absolute encoders:** Thrifty 10-pin magnetic encoder plugged into SPARK MAX data port (`type: attached` — id field ignored, reads automatically through SPARK MAX)

**Gyroscope:** ADIS16470 on roboRIO SPI port (id: 0 in YAGSL config)

---

## Flywheel

| Device | Type | CAN ID |
|--------|------|--------|
| Left Vortex (primary, PID master) | NEO Vortex via SPARK Flex | **23** |
| Right Vortex (follows left)       | NEO Vortex via SPARK Flex | **22** |
| Front Feed Wheel                  | NEO Brushless via SPARK MAX | **15** |
| Back Feed Wheel                   | NEO Brushless via SPARK MAX | **2**  |

---

## Intake

| Device | Type | CAN ID |
|--------|------|--------|
| Left Arm  | NEO Brushless via SPARK MAX | **16** |
| Right Arm | NEO Brushless via SPARK MAX | **17** |
| Intake Wheel | NEO Brushless via SPARK MAX | **4** |

Note: Left and right arm PIDs are independent (no follow) due to mechanical looseness in the arm assembly.

---

## Conveyor / Spindexer

| Device | Type | CAN ID |
|--------|------|--------|
| Conveyor Belt Motor | Brushed motor via SPARK MAX | **21** |
| Spindexer Motor     | NEO Brushless via SPARK MAX | **18** |

---

## Climber

| Device | Type | CAN ID |
|--------|------|--------|
| Vertical Motor | NEO Brushless via SPARK MAX | **11** |

---

## Side Claw

| Device | Type | CAN ID |
|--------|------|--------|
| Claw Motor | NEO Brushless via SPARK MAX | **20** |

> CAN ID conflict resolved: SideClaw moved from **18** → **20** (was conflicting with Conveyor Spindexer). Flash the SideClaw SPARK MAX to ID 20 via REV Hardware Client before enabling.

---

## Hardware Summary

| Parameter | Value |
|-----------|-------|
| Module | Thrifty Swerve |
| Drive motor | NEO Brushless (REV) |
| Steer motor | NEO Brushless (REV) |
| Motor controller | SPARK MAX (REV) |
| Drive gear ratio | 6.23:1 |
| Steer gear ratio | 25:1 |
| Wheel diameter | 4 inches |
| Module location | ±11 inches (center-to-center: 22" × 22") |
| Gyroscope | ADIS16470 (roboRIO SPI) |
| Limelight | LL3, hostname: `limelight` |
| LED strip | 60 LEDs, PWM port 0 |

---

## Encoder Offsets

All offsets are currently **0.0** — not yet calibrated on hardware.

**🔴 Must calibrate before driving** — otherwise the robot won't drive straight.

**To calibrate:**
1. Deploy to robot, open SmartDashboard / Shuffleboard / Elastic
2. Point all modules forward (bevel gears pointing one consistent direction — see YAGSL docs)
3. Read `Drive/Module X/Absolute Encoder Position` for each module
4. Update `"absoluteEncoderOffset"` in each `deploy/swerve/modules/<pos>.json` directly
   (or if running via `python tools/generate_configs.py`: update `[encoder_offsets]` in
   `hardware_config.ini` and re-run the generator)
5. Redeploy

See `PRACTICE_SESSION_PLAYBOOK.md` Phase A for the pre-drive checklist that covers this.

## SideClaw CAN ID — must reflash before first drive

SideClaw SPARK MAX needs to be flashed from ID **18 → 20** via REV Hardware Client.
Code references `Constants.*.kClaw* = 20`; the physical module is still at 18 from last
season. Procedure: REV Hardware Client → select the SideClaw Spark → Change CAN ID → 20
→ save → power-cycle.
